# Performance Plan
- SSR critical pages; dynamic import() for heavy widgets
- Use AVIF/WEBP and proper caching
- Measure with Lighthouse (mobile) aiming > 95

Run local check:

```bash
./scripts/lighthouse.sh https://localhost:3000
```
